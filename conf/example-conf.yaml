no_queries: 1000
no_of_similar_query_groups: 4
min_no_of_operators: 2
max_no_of_operators: 4
should_have: [ 'Window', 'Join' ]
source_list:
  - stream_name: 'example'
    int_fields: [ 'id', 'val' ]
    string_fields: [ ]
    double_fields: [ ]
    timestamp_fields: [ ]
filter_list:
  - on_schema: a
    on_fields:
      - filed_name: 'id'
        type: ''
        max_value:
        min_value:

